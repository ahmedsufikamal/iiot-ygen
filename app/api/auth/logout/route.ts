import { NextResponse } from 'next/server'; export const runtime='nodejs'; export const dynamic='force-dynamic'; export async function POST(){ const res=NextResponse.json({ok:true}); res.cookies.set({name:'demo_auth',value:'',path:'/',maxAge:0}); res.cookies.set({name:'tenant',value:'',path:'/',maxAge:0}); res.cookies.set({name:'role',value:'',path:'/',maxAge:0}); res.cookies.set({name:'email',value:'',path:'/',maxAge:0}); return res; }
