import { store } from '@/lib/store'; export const runtime='nodejs'; export const dynamic='force-dynamic'; export async function POST(req:Request){ const body=await req.json().catch(()=>({})); const { deviceId, notes } = body as { deviceId?:string; notes?:string }; if(!deviceId) return Response.json({error:'deviceId required'},{status:400}); const wo=store.createWorkOrder(deviceId,notes); return Response.json({ ok:true, workOrder:wo }); }
