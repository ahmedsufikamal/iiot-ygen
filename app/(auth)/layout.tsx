import '../globals.css'; export const metadata={title:'Sign in — YGEN IIoT Telematics'}; export default function AuthLayout({children}:{children:React.ReactNode}){ return (<html lang='en'><body><div className='auth-wrap'>{children}</div></body></html>); }
